{
  "name": "@storagepk/worker",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "start": "node dist/main.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/jobs.test.cjs"
  },
  "dependencies": {
    "@storagepk/contracts": "0.1.0",
    "@storagepk/database": "0.1.0",
    "bullmq": "^5.30.1",
    "pino": "^9.6.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.7.3"
  }
}
